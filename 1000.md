# Top-5 全排列顺序敏感性诊断：实验结论（1000 题，obsexp.md）

> 设置：`results/obs.md`；详细表/图：`results/perm_diag/analysis_summary.md` + `distance_bucket_summary.csv` + `reverse_tail_analysis.csv` + `kendall_vs_reward_drop.pdf` + `kendall_vs_hcf.pdf`。
>
> **Backbone：Llama-3.1-8B-Instruct (base，无 RL)** · greedy（temp=0）· **1000 题**（eval_1000 池 4000, seed=42 取前 1000，含先前 200/500 为子集）× 5!=120 全排列 = **120,000 次推理** · 8 路数据并行（A 方案，8× vLLM TP=1）。reward/EM/F1 全复用项目逻辑未改。Kendall 距离 = 相对检索序逆序数（0–10，reverse=10）。本结论为 **1000 题最终版**，如实呈现未筛选。

## 一句话结论
**「Ranking 位移越大 → 行为退化越大」成立但弱且仅在"模型本就答对"的题上——1000 题下逐题 Spearman 终于显著（ρ≈+0.034, CI>0），聚合上 Reverse 为退化峰值（reward/F1/HCF 三冠）。Reverse 是"平均最害"的合理单一 counterfactual（最大位移↔最大平均退化 得证），却非逐题 worst-case proxy（per-question percentile≈中位、Tail 收敛到随机基线）。**

## 关键数字（1000 题，retrieval-correct n=275，27.5%）

**Analysis 1 — 按 Kendall 距离聚合（retrieval-correct）**

| Kendall d | 0 | 2 | 4 | 6 | 8 | 10(reverse) |
|---|---|---|---|---|---|---|
| reward_drop | 0 | 0.106 | 0.126 | 0.135 | 0.148 | **0.163** |
| F1_drop | 0 | 0.124 | 0.148 | 0.159 | 0.175 | **0.192** |
| HCF % | 0 | 14.8 | 18.1 | 19.5 | 21.4 | **22.9** |

- retrieval-correct 子集：三项**随位移单调升至 reverse 取峰值**（1000 比 500 更干净）。
- 全部 1000 题：reward_drop **持平为负**（−0.004~−0.011）——非检索序排列平均 reward 略高，总体无退化趋势。

**Analysis 2 — per-question Spearman(Kendall, RewardDrop)**

| 子集 | mean ρ | median ρ | 95% CI | 正向比例 | n |
|---|---|---|---|---|---|
| 全部题 | −0.013 | −0.011 | **[−0.025, −0.0004]** | 46.3% | 1000 |
| retrieval-correct | +0.034 | +0.033 | **[+0.010, +0.058]** | 54.9% | 275 |

→ **1000 题终于显著**：retr-correct ρ=+0.034 **CI 完全>0**（200/500 时跨 0）；全题 ρ=−0.013 **CI 完全<0**（微负显著）。

**Analysis 3 — Reverse harmful percentile（retrieval-correct 275 题）**

| 指标 | 1000 | 随机基线 |
|---|---|---|
| mean / median reverse percentile | **52.5 / 48.3** | 50 |
| Tail@5% | 4.4% | 5% |
| Tail@10% | **9.1%** | 10% |
| Tail@20% | 17.8% | 20% |
| HCF：Reverse / Average / Random | **22.9%** / 18.4% / 17.5% | — |

→ reverse percentile **≈中位**，**Tail 率收敛到随机基线**（9.1%≈10%）；但 reverse 单点 HCF 22.9% 明显高于 avg/random，且聚合三项峰值。

## RQ1：位移越大是否越退化？
**成立但弱、仅限答对的题。**
- ✅ 聚合：retr-correct reward_drop 0→0.163、F1_drop 0→0.192、HCF 0→22.9% 单调升至 reverse 峰值。
- ✅ **逐题（1000 显著）**：retr-correct Spearman ρ=+0.034，CI [+0.010,+0.058] > 0 → 逐题层面"位移↑→退化↑"**统计显著（弱）**。200/500 跨 0 属小样本波动，n=275 后稳定 ρ≈+0.03 并显著。
- ❌ 全题 ρ=−0.013（CI<0，微负显著）：错题上换序略涨 reward（检索序非最优）。
- 结论：**位移-退化正相关真实但弱、仅存在于 retrieval-correct 题**；全题上被错题稀释为微负。

## RQ2：Reverse 是否稳定落入 harmful tail、可作训练用单一 counterfactual？
**Reverse 是平均最害的合理单一 counterfactual、最大位移↔最大平均退化得证；但非逐题 worst-case proxy。**
- ✅ 聚合三项（reward_drop/F1_drop/HCF）reverse 均峰值；HCF 22.9% > avg/random(~18%)。**"最大化 Kendall 位移 ↔ 最高平均行为退化"在 1000 题确认**（三样本量下随 n 增大更干净）。
- ❌ 逐题 percentile ≈52（中位），**Tail@5/10/20% = 4.4/9.1/17.8% 收敛到随机基线** → reverse **不比随机排列更常落入单题 harmful tail**。
- 不矛盾：reverse"平均最害"（系统性偏高 + 部分题拉高）但 reward_drop 并列 + 单题内常存同等/更害排列 → **单题排名常居中**。
- 结论：reverse **可作合理单一结构化 counterfactual**（平均最害、构造确定、覆盖全位移、HCF 偏高、最大位移↔最大平均退化成立），**但绝不应作逐题 worst-case proxy**——用 reverse 做 hard negative 会系统性低估小位移有害排列。

## 200 / 500 / 1000 演进（稳健性定稿）
| 量 | 200 | 500 | 1000 | 趋势 |
|---|---|---|---|---|
| retr-correct n（率） | 47(23.5%) | 136(27.2%) | 275(27.5%) | 率稳 ~27% |
| reverse 聚合峰? | 否(kd8) | 是 | 是（更干净） | n↑→reverse 转峰且单调 |
| retr-correct ρ（CI） | +0.056(跨0) | +0.029(跨0) | **+0.034(>0✓)** | n↑→稳定 +0.03 并显著 |
| 全题 ρ（CI） | −0.001(跨0) | −0.011(跨0) | **−0.013(<0✓)** | n↑→微负显著 |
| reverse percentile | 48.4 | 53.0 | 52.5 | 稳定 ≈中位 |
| Tail@10% | 6.4% | 8.1% | **9.1%≈基线** | n↑→收敛随机基线 |
| HCF rev/avg/rand | 17.0/14.7/8.5 | 19.1/15.9/16.9 | 22.9/18.4/17.5 | reverse 持续偏高 |

→ n 增大**确认四点**：①聚合位移-退化单调、reverse 为峰；②逐题正相关弱但**显著**（仅答对题）；③reverse 非逐题 tail（tail≈随机）；④全题微负（检索序非最优）。**1000 题为定稿结论。**

## 对本研究的含义
1. **Reverse 作训练单一 counterfactual 得到最强支持**：1000 题下它在聚合意义上确为最害（reward/F1/HCF 三峰）、覆盖全位移、构造确定、"最大位移↔最大平均退化"成立——作为单一结构化 hard 样本站得住。
2. **但仍非逐题 worst-case proxy**：单题内常有位移更小却同等/更害的排列 + reward_drop 并列，使 reverse 单题排名中位、tail≈随机。训练若只用 reverse，**系统性低估小位移有害排列** → 强支持 §4.2 four_orders / 多排列采样方向。
3. **顺序鲁棒性优化主战场是"本就答对"的题**：位移-退化效应（聚合+逐题显著）仅存于 retrieval-correct 子集（27.5%），全题上为微负——与本工作 HCF 口径（P(reverse错|forward对)）完全一致，**强力支持续用 HCF 作优化目标**。
4. **检索序非全局最优**：全题 reward_drop 为负且 n=1000 下显著 → reranker 在 base 上 +1–1.4 EM（§3.2）得到行为佐证。
5. **效应量提醒**：逐题 ρ≈+0.034（虽显著）很小——位移"解释"的退化方差有限，顺序只是眾多因素之一；不应夸大顺序鲁棒性的优化上限。

## 局限
- 单 backbone（base Llama）、单种子采样（seed=42；推理 greedy 确定性）。1000 题（retr-correct 275）下 RQ1 逐题已显著、tail 已收敛——主要结论稳健；若需跨模型泛化可在 Gemma2/Qwen3 复测 reverse 聚合峰性。
- reward_drop 含 cite 项，但三样本量下 reward_drop/F1_drop/HCF 三维度峰值一致落 reverse，结论对度量选择稳健。
